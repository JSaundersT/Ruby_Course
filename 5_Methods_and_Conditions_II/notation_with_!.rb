user = "free"

if user != "subscriber"
  puts "Only subscribers are allowed"
end


puts !1 #converts to boolean then flips it

puts !!1 #converts to boolean then flips it twice

#the purpose of this is to convert anything into a boolean
#and then converting it back to the correct output
